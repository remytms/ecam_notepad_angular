# ecam_notepad_angular
ECAM Project: a notepad website in Angular 2

# How to install
After cloning with git run in the directory of the project :

    npm install

Then run the appliaction with :

    npm start
